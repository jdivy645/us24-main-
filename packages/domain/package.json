{
  "name": "@us24/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical VOB field registry, rule engine, normalization, deterministic comparison and status precedence. Pure TypeScript, zero I/O.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "3.2.4"
  }
}
