{
  "name": "@us24/testing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Golden fixtures and test factories. Fixture loading is behind an interface so client source files can replace the reconstruction without touching engine or test code.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@us24/domain": "workspace:*"
  }
}
